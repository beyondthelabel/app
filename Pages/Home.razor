@page "/"
@using System.Collections.Generic

<style>
    /* General Styles */
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
    }

    /* Introduction Section */
    .intro-section {
        position: relative;
        height: 100vh;
        overflow: hidden;
        color: white;
        text-align: center;
    }

    .background-video {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        z-index: -1;
    }

    .intro-content {
        position: relative;
        z-index: 1;
        top: 50%;
        transform: translateY(-50%);
        width: 100%;
    }

    .intro-section h1 {
        font-size: 3rem;
    }

    .intro-section p {
        font-size: 1.2rem;
    }

    /* Skills Section */
    .skills-section {
        padding: 50px 0;
        background-color: #f8f9fa;
        text-align: center;
    }

    .skills-section h2 {
        margin-bottom: 30px;
    }

    .skill-box {
        background-color: white;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        padding: 20px;
        transition: transform 0.3s ease;
    }

    .skill-box:hover {
        transform: translateY(-10px);
    }

    /* Projects Section */
    .projects-section {
        padding: 50px 0;
        text-align: center;
    }

    .projects-section h2 {
        margin-bottom: 30px;
    }

    .project-card {
        background-color: #f8f9fa;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        margin-bottom: 30px;
        padding: 20px;
        transition: transform 0.3s ease;
    }

    .project-card:hover {
        transform: translateY(-10px);
    }

    .project-card h4 {
        font-size: 1.5rem;
    }

    .project-card a {
        margin-top: 10px;
    }

    /* Contact Section */
    .contact-section {
        padding: 50px 0;
        background-color: #f8f9fa;
        text-align: center;
    }

    .contact-section h2 {
        margin-bottom: 20px;
    }

    .contact-section p {
        font-size: 1.2rem;
        margin-bottom: 20px;
    }

    .contact-section .btn {
        margin-top: 10px;
    }

    .hero-title {
        font-size: 2.5rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .highlight-bar {
        width: 80px;
        height: 4px;
        background-color: #6a1b9a;
        margin: 20px auto;
    }

    .description {
        font-size: 1.2rem;
        line-height: 1.8;
        margin-top: 20px;
    }


</style>

<main>
    <!-- Introduction Section with Video -->
    <section id="intro" class="intro-section">
        <!-- Video Background -->
        <video autoplay loop muted playsinline class="background-video">
            <source src="media/videos/landing-page-video.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>

        <!-- Overlay Content -->
        <div class="intro-content">
            <h1 class="hero-title">Welcome to <br /> <span class="text-dark">Beyond</span> <span class="text-primary">The</span> <span class="text-secondary">Label</span></h1>

            @*             <h1>Welcome! My name is <strong>Jada Augustine</strong></h1>
            <p class="lead">I am passionate about making an impact, preserving culture, and fostering growth within communities.</p>
            <p>
            Through my work, I strive to bridge gaps in education, celebrate heritage, and create meaningful change that uplifts individuals and communities alike.
            </p> *@
        </div>

    </section>
    <section id="description">
        <div class="container mt-5">
            @*             <div class="text-center">
            <h1 class="hero-title">Welcome to <br /> <span class="text-dark">Beyond The Label</span></h1>
            <div class="highlight-bar"></div>
            </div> *@
            <div class="mt-4">
                <p class="description">
                    <strong>Beyond The Label</strong> is an international speech-language pathology practice offering virtual
                    speech therapy sessions across the United States. Headquartered in Atlanta, GA, we're dedicated to
                    providing top-tier therapeutic interventions for individuals of all ages and abilities. Since our
                    founding as a clinical practice in 2016, Beyond The Label has evolved into a multifaceted organization,
                    offering healthcare, therapy, professional development, and educational, business, and career services.
                    Our team provides comprehensive virtual and in-person services committed to enhancing personal and
                    professional growth.
                </p>
                <p class="description">
                    Our service locations include Washington, D.C., Maryland, Virginia, Georgia, Louisiana, California,
                    North Carolina, New Jersey, Arizona, and Maine.
                </p>
            </div>
        </div>
    </section>

    <!-- Skills Section -->
    @*     <section class="skills-section">
    <div class="container">
    <h2>Skills</h2>
    <div class="row">
    @foreach (var skill in skills)
    {
    <div class="col-md-4">
    <div class="skill-box">
    <i class="bi bi-check-circle"></i>
    <h4>@skill.Name</h4>
    <p>@skill.Description</p>
    </div>
    </div>
    }
    </div>
    </div>
    </section> *@

    <!-- Projects Section -->
    <section id="projects" class="projects-section">
        <div class="container">
            <h2>Services Provided</h2>
            <div class="row row-cols-1 row-cols-md-3 g-4">
                @foreach (var project in projects)
                {
                    <div class="col">
                        <div class="card h-100 project-card">
                            <!-- <img src="..." class="card-img-top" alt="..."> -->
                            <div class="card-body">
                                <h5 class="card-title">@project.Title</h5>
                                <p class="card-text">@project.Description</p>
                            </div>
                            <div class="card-footer">
                                <button class="btn btn-primary">Learn More</button>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </section>

    <section id="calendy">
        <!-- Calendly inline widget begin -->
        <div class="calendly-inline-widget" data-url="https://calendly.com/beyondthelabellosangeles?text_color=0f2eec&primary_color=74a8f4" style="min-width:320px;height:700px;"></div>
        <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
        <!-- Calendly inline widget end -->
    </section>

</main>

@code {
    // Sample skills data
    private List<Skill> skills = new List<Skill>
    {
        new Skill { Name = "C#", Description = "Proficient in writing clean, maintainable code." },
        new Skill { Name = "ASP.NET Core", Description = "Experienced in building web applications using .NET." },
        new Skill { Name = "Blazor", Description = "Developing modern web apps with Blazor." },
    };

    // Sample projects data
    private List<Project> projects = new List<Project>
    {
        new Project { Title = "What we do example 1", Description = "Another awesome project using Blazor.", Link = "https://example.com/project2" },
        new Project { Title = "What we do example 2", Description = "Mobile app built using Xamarin.", Link = "https://example.com/project3" },
        new Project { Title = "What we do example 3", Description = "Mobile app built using Xamarin.", Link = "https://example.com/project3" },
        new Project { Title = "What we do example 4", Description = "Mobile app built using Xamarin.", Link = "https://example.com/project3" },
        new Project { Title = "What we do example 5", Description = "Mobile app built using Xamarin.", Link = "https://example.com/project3" },
        new Project { Title = "What we do example 6", Description = "Mobile app built using Xamarin.", Link = "https://example.com/project3" }
    };

    // Classes for managing skills and projects
    private class Skill
    {
        public string Name { get; set; }
        public string Description { get; set; }
    }

    private class Project
    {
        public string Title { get; set; }
        public string Description { get; set; }
        public string Link { get; set; }
    }
}


<script>
    try {
        // Select all sections
        const sections = document.querySelectorAll("section");

        // Intersection Observer logic
        const observer = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add("visible");
                }
            });
        }, { threshold: 0.1 });

        // Observe each section
        sections.forEach(section => {
            observer.observe(section);
        });
    } catch (error) {
        console.error("Error observing sections:", error);
    }
</script>